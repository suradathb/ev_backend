# ------------------------
# App basic config
# ------------------------
PROJECT_NAME=EV Service Center Management API
SECRET_KEY=super_secret_key_change_me
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ------------------------
# Database backend switch
# เลือก mysql หรือ postgres
# ------------------------

# ถ้าอยากใช้ MySQL + phpMyAdmin:
# DB_BACKEND=mysql

# ถ้าอยากใช้ PostgreSQL:
DB_BACKEND=postgres

# ------------------------
# MySQL connection (ใช้ตอน DB_BACKEND=mysql)
# ------------------------
MYSQL_URL=mysql+pymysql://root:@localhost:3306/ev_service_db

# ------------------------
# PostgreSQL connection (ใช้ตอน DB_BACKEND=postgres)
# ------------------------
# ❗ ถ้าจะใช้ asyncpg จริง ๆ:
# POSTGRES_URL=postgresql+asyncpg://appuser:secret123@localhost:5432/appdb
# แต่แนะนำ sync ก่อน:
POSTGRES_URL=postgresql+psycopg2://appuser:secret123@localhost:5432/ev_service_db
